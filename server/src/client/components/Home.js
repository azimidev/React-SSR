import React from "react";

export default () => {
    return (
        <div>
            <h1>Home</h1>
            <button onClick={() => console.log('Hello World')}>Click Me</button>
        </div>
    );
};
